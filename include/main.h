#pragma once

// Optional header the library "owns". The executable doesn't need this for modules,
// but it's here because you asked to keep it in the lib project.
//
// You can put non-module declarations used by lib_main.cpp here if you want.
